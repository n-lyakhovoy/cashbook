# Cashbook - Приложение для учета наличных

## Что создано

Полнофункциональное веб-приложение на **Laravel 12** + **MariaDB** с Docker, включающее:

### ✅ Реализованные функции

1. **Аутентификация и RBAC**
   - Регистрация и логин (Laravel Breeze)
   - Три роли: super-admin, admin-write, admin-read
   - Google Authenticator 2FA
   - Восстановление пароля по email
   - Spatie Permission для управления правами

2. **Управление поступлениями наличных**
   - Добавление записей о поступлении денег
   - Указание суммы, источника, даты/времени
   - Отслеживание администратора, принявшего деньги
   - История и статистика

3. **Управление сотрудниками**
   - CRUD операции (добавление, редактирование, удаление)
   - Данные: ФИ, отдел, должность, оклад, приоритет (1-5)

4. **Расчетные ведомости (Payroll)**
   - Месячный расчет зарплаты по каждому сотруднику
   - Редактирование бонусов, штрафов, авансов, отпускных
   - Автоматический расчет: оклад + бонусы - штрафы - аванс - офиц. ЗП - отпускные
   - Цветовое кодирование (зеленый - 0 остатка, желтый - есть остаток, красный - минус)

5. **Управление выплатами**
   - Выплата наличных сотрудникам из доступного остатка
   - Автоматическое уменьшение средств в кассе
   - История выплат по месяцам

6. **Система уведомлений**
   - Email и Push уведомления администраторам
   - Настройки: кому и когда отправлять (при поступлении / выплате)
   - Интеграция с Mailpit для локального тестирования

7. **Двухфакторная аутентификация**
   - Google Authenticator QR-код
   - Включение/отключение 2FA в профиле

### 📦 Стек технологий

- **Backend**: Laravel 12, PHP 8.4
- **Database**: MariaDB
- **Cache**: Redis
- **Auth**: Laravel Breeze (Blade), Spatie Permission, Google2FA
- **Frontend**: Blade templates, Alpine.js, Tailwind CSS
- **Mail**: Mailpit (локально), SMTP (продакшен)
- **Containerization**: Docker, Docker Compose

### 📂 Структура проекта

```
cashbook/
└── src/
    ├── app/
    │   ├── Models/
    │   │   ├── User.php
    │   │   ├── Employee.php
    │   │   ├── CashEntry.php
    │   │   ├── Payout.php
    │   │   ├── PayrollMonthly.php
    │   │   ├── Notification.php
    │   │   └── Setting.php
    │   ├── Http/Controllers/
    │   │   ├── DashboardController.php
    │   │   ├── EmployeeController.php
    │   │   ├── CashEntryController.php
    │   │   ├── PayrollController.php
    │   │   ├── PayoutController.php
    │   │   └── Auth/TwoFactorController.php
    │   └── Http/Middleware/
    │       └── Check2FA.php
    ├── database/
    │   ├── migrations/
    │   │   └── (все миграции для таблиц)
    │   └── seeders/
    │       ├── RoleSeeder.php
    │       ├── AdminSeeder.php
    │       ├── EmployeeSeeder.php
    │       └── CashEntrySeeder.php
    ├── resources/views/
    │   ├── dashboard.blade.php
    │   ├── employees/ (index, create, edit)
    │   ├── cash/ (index, create)
    │   └── payroll/ (index с модальными окнами)
    ├── routes/web.php
    └── README.md
```

### 🗄️ База данных

**11 таблиц**:
- `users` - администраторы (с google2fa_secret, google2fa_enabled)
- `employees` - сотрудники (last_name, first_name, department, position, salary, priority)
- `cash_entries` - поступления (amount, source, received_at, admin_id)
- `payouts` - выплаты (employee_id, amount, paid_at, admin_id)
- `payroll_monthlies` - месячные расчеты (salary, bonus, penalty, advance, official_salary, vacation, to_cash, paid, remaining)
- `notifications` - уведомления (type, message, user_id, is_email, is_push, is_sent)
- `settings` - настройки (user_id, receive_on_intake, receive_on_payout)
- Плюс таблицы Spatie Permission (roles, permissions, etc.)

## 🚀 Быстрый старт

### Запуск приложения

```bash
cd src

# 1. Запустить контейнеры
./vendor/bin/sail up -d

# 2. Применить миграции и загрузить сиды
./vendor/bin/sail artisan migrate:fresh --seed

# 3. (Опционально) Горячая перезагрузка фронтенда
./vendor/bin/sail npm run dev
```

Приложение доступно на: **http://localhost**
Mailpit для тестирования email: **http://localhost:8025**

### Учетные данные

| Роль | Email | Пароль | Права |
|------|-------|--------|-------|
| Super Admin | superadmin@cashbook.local | password | Все |
| Admin (RW) | admin@cashbook.local | password | Касса, сотрудники, зарплата |
| Admin (Read) | admin-read@cashbook.local | password | Только просмотр |

## 📋 Основные маршруты

- `GET /dashboard` - главная страница
- `GET|POST /employees` - управление сотрудниками
- `GET|POST /cash` - управление поступлениями
- `GET|POST /payroll` - расчетные ведомости
- `POST /payouts` - выплаты
- `GET /two-factor` - управление 2FA
- `GET /profile` - профиль пользователя

## 🛠️ Полезные команды

```bash
cd src

# Миграции
./vendor/bin/sail artisan migrate              # Применить миграции
./vendor/bin/sail artisan migrate:fresh        # Переприменить все
./vendor/bin/sail artisan migrate:fresh --seed # + загрузить сиды

# Сиды
./vendor/bin/sail artisan db:seed

# Кэш
./vendor/bin/sail artisan cache:clear
./vendor/bin/sail artisan config:clear

# Логи
./vendor/bin/sail logs -f laravel

# Tinker (REPL)
./vendor/bin/sail artisan tinker

# Тесты
./vendor/bin/sail artisan test

# Контейнеры
./vendor/bin/sail up -d      # Запуск
./vendor/bin/sail down       # Остановка
./vendor/bin/sail bash       # Доступ в контейнер
```

## 🔧 Конфигурация

### .env переменные

```
APP_NAME=Cashbook
APP_ENV=local
APP_DEBUG=true
APP_URL=http://localhost

DB_CONNECTION=mysql
DB_HOST=mysql
DB_PORT=3306
DB_DATABASE=cashbook
DB_USERNAME=sail
DB_PASSWORD=password

MAIL_MAILER=mailpit
MAIL_HOST=mailpit
MAIL_PORT=1025
```

## 📝 Особенности реализации

1. **Чтение-Запись для admin-write**:
   - Может добавлять поступления и выплачивать сотрудникам
   - Может управлять сотрудниками и зарплатой
   - Получает уведомления (если включено в settings)

2. **Только чтение для admin-read**:
   - Может просматривать все данные
   - Не может создавать, редактировать, удалять
   - НЕ получает уведомления (флаги отключены по умолчанию)

3. **Автоматический расчет "к выдаче наличными"**:
   - Формула: `salary + bonus - penalty - advance - official_salary - vacation`
   - Пересчитывается при сохранении

4. **Цветовое кодирование в Payroll**:
   - Зеленый: `remaining = 0` (все выплачено ровно)
   - Желтый: `remaining > 0` (есть остаток к выплате)
   - Красный: `remaining < 0` (перерасход)

5. **Проверка лимита касс**:
   - При выплате проверяется, достаточно ли средств: `total_cash - total_paid >= amount_to_pay`
   - Если нет — выплата отклоняется с ошибкой

## 🔐 Безопасность

- Пароли хешированы (bcrypt)
- CSRF защита (Laravel)
- SQL инъекции предотвращены (Eloquent ORM)
- Авторизация через `$this->authorize('permission')`
- 2FA для дополнительной защиты администраторов

## 📧 Email и уведомления

- Уведомления отправляются при:
  - Добавлении поступления наличных (если `receive_on_intake = true`)
  - Выплате сотруднику (если `receive_on_payout = true`)
- Локально письма можно просмотреть в **Mailpit** (`http://localhost:8025`)
- На продакшене используется реальный SMTP (Gmail, SendGrid, и т.д.)

## 🎯 Что можно улучшить в будущем

1. **REST API** для мобильного приложения
2. **Web Push уведомления** (реализовать через Service Workers)
3. **Экспорт в PDF/Excel** для отчетов
4. **Двусторонняя синхронизация** с бухгалтерской программой
5. **SMS уведомления** для критических операций
6. **Аудит логирование** всех действий администраторов
7. **Реал-тайм оповещения** через WebSockets
8. **Backup автоматический** базы данных
9. **Уличение попыток взлома** и защита от brute-force
10. **Интеграция с банком** для автоматического учета поступлений

## 📞 Контакты и поддержка

Приложение готово к использованию. При возникновении вопросов обратитесь к разработчику.

---

**Версия**: 1.0  
**Дата создания**: Декабрь 2025  
**Язык**: PHP 8.4, Laravel 12, MariaDB
